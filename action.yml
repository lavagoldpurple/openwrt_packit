name: Package E20C OpenWrt image
description: Package a pinned Armv8 rootfs and rk35xx kernel into an E20C image.
runs:
  using: composite
  steps:
    - shell: bash
      run: sudo "${GITHUB_ACTION_PATH}/openwrt_flippy.sh"
